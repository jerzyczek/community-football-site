{% extends 'base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Create new Post</h1>

        {{ include('post/_form.html.twig') }}

        <a href="{{ path('post_index', {'groupId' : app.request.get('groupId')}) }}"><button class="btn btn-info back-to-list-button">Back to previous page</button></a>
    </div>
{% endblock %}

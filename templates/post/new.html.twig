{% extends 'base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
    <h1>Create new Post</h1>

    {{ include('post/_form.html.twig') }}

    <a href="{{ path('post_index', {'groupId' : app.request.get('groupId')}) }}">back to list</a>
{% endblock %}
